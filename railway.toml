[build]
builder = "NIXPACKS"
buildCommand = "cd clamped-ui && npm install && npm run build && cd .. && mvn -pl clamped-core,clamped-server -am package -DskipTests"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
